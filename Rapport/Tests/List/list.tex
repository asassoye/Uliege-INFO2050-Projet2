\documentclass[tikz,border=2mm]{standalone}
\usepackage{pgfplots}
\usepgfplotslibrary{units}

\usepackage{filecontents}
% the filecontents environment writes its content to the specified file

\begin{document}
\begin{tikzpicture}
\begin{axis}[
  legend style = { at = {(0.6,0.75)}},
  unit markings=slash space,
  x unit={},
  y unit={sec},
  xlabel=Size,
  ylabel=Time,
  xmin=0,xmax=160,
  ymin=0,ymax=40,
  ytick={0,10,...,40},
  xtick={0, 20,...,160},
  grid,
  grid style={dotted}]

\addplot [thin] table {list.dat};

\end{axis}
\end{tikzpicture}
\end{document}